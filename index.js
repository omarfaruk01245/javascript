const a = 20;
console.log(a);

const numbar = [20, 23, 15, 30];
numbar.filter((x) => x > 15);
console.log(numbar);

// if(numbar >20)
//  console.log(numbar)

const age = 20;

if (age => 25) {
 'Ticket discount 5%'
} else {
'10%'
}

console.log(age);


function testNum(a) {
    let result;
    if (a > 0) {
      result = 'positive';
    } else {
      result = 'NOT positive';
    }
    return result;
  }
  
  console.log(testNum(-5));